import 'package:flutter/material.dart';


class AppColors {
  AppColors._(); 

  static const Color primary = Color(0xFF3D63DD); 
  static const Color secondary = Color(0xFFE5EEFF); 

  // Definisikan warna pendukung (Utility)
  static const Color background = Color(0xFFFDFDFE); // Abu-abu muda
  static const Color textDark = Color(0xFF27252E); 
  static const Color textLight = Color(0xFFFFFFFF);
  
  // Definisikan warna status
  static const Color success = Color(0xFF28A745);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC3545);
}